import { Box } from "@mui/material"
import React, { memo } from "react"

const FormStaking = () => (
  <Box className="h-[40.25vh] max-h-[644px]">Form Staking</Box>
)
export default memo(FormStaking)
